spring:
  ai:
    openai:
      api-key: ${IFLOW_API_KEY}  # 环境变量，别写死在代码里
      base-url: https://apis.iflow.cn
      chat:
        options:
          model: glm-4.6
          temperature: 0.7

server:
  port: 8080
