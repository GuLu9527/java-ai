spring:
  ai:
    openai:
      api-key: ${IFLOW_API_KEY}
      base-url: https://apis.iflow.cn
      chat:
        options:
          model: qwen3-coder-plus
          temperature: 0.7

server:
  port: 8080
